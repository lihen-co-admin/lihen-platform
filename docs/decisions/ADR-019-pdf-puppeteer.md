# ADR-019 — PDF mediante HTML/CSS + Puppeteer

**Estado:** Aprobado

El motor del catálogo V5 se implementará como layout reproducible en HTML/CSS y exportación server-side mediante Puppeteer. No forma parte del bootstrap 1.0.
