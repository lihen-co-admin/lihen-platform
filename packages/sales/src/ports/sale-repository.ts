import type{CompleteOrderSaleInput,CreatePosSaleInput,Sale,SaleItem}from'../domain/sale';export interface SaleRepository{list():Promise<readonly Sale[]>;listItemsBySaleIds(saleIds:readonly string[]):Promise<readonly SaleItem[]>;createPos(input:CreatePosSaleInput):Promise<Sale>;completeOrder(input:CompleteOrderSaleInput):Promise<Sale>;}
