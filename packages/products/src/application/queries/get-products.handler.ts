import type { ProductListItemDTO } from '../dto/product-list-item.dto'; import type { ProductRepository } from '../../ports/product-repository'; import type { BrandRepository } from '../../ports/brand-repository'; import type { CategoryRepository } from '../../ports/category-repository'; import { resolveProductTaxonomy } from '../product-taxonomy-resolver'; import type { GetProductsQuery } from './get-products.query';
export class GetProductsHandler { public constructor(private readonly productRepository:ProductRepository,private readonly brands?:BrandRepository,private readonly categories?:CategoryRepository){} public async execute(_query:GetProductsQuery):Promise<readonly ProductListItemDTO[]>{ const products=await this.productRepository.findAll(); return Promise.all(products.map(async product=>{ const t=await resolveProductTaxonomy(product,this.brands,this.categories); return {id:product.id,sku:product.sku??null,catalogCode:product.catalogCode??null,slug:product.slug,name:product.name,businessLine:product.businessLine,brandId:product.brandId??null,brandName:t.brandName??null,categoryId:product.categoryId??null,categoryName:t.categoryName??null,status:product.status,salePrice:{amount:product.salePrice.amount,currency:product.salePrice.currency}}; })); } }
